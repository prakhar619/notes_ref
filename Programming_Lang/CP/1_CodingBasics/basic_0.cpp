#include <bits/stdc++.h>
using namespace std;
typedef long long ll;   //now ll x = LONG_LONG_MAX;
typedef vector<int> vi; // vi y(10,1);
//Macros
#define   f(i,x,y,z) for(int i = x; i <= y; i+=z) 

int workingWithNumbers()
{
    cout << "int is 32 bit. Max value:" << INT_MAX << "\n";
    cout << "long is also 32 bit. Max value:"<< LONG_MAX << "\n";
    cout << "Use long long as it is 64 bit. Max value:" << LONG_LONG_MAX << "\n";
    long long int r =  LONG_LONG_MAX;
    // also int*int = int
    // use LL;
    int a = 1;
    long long k =(long long) a*a;

    unsigned int v = -1;

    //never compare float/double with int or another float directly
    //always round off to some decimal digit

    return 1;
}

int main()
{
    cout << "Use \\n instead of endl as it causes flushing" << "\n";

    int inp1,inp2;
    cin >> inp1;
    cin >> inp2;

    string s;
    getline(cin,s);
    //s is now full line

    workingWithNumbers();
    

    return 1;
}